extends layouts/login-layout.pug

block content
    h1 Login
    form(method="post") 
        input#usernameInput(type="text", name="logUsername", placeholder="Enter username", required="")
        input(type="submit", value="Login")
    a(href="/register") Create Account